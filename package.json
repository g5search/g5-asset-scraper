{
  "name": "g5-asset-scraper",
  "version": "1.3.0",
  "description": "",
  "main": "index.js",
  "config": {
    "imageRepo": "gcr.io/g5-images/asset-scraper",
    "imageName": "asset-scraper"
  },
  "scripts": {
    "docker:build": "docker build -t $npm_package_config_imageName .",
    "docker:run": "docker run -p 8085:8085 $npm_package_config_imageName",
    "docker:tag:prime": "branch=$(git rev-parse --symbolic-full-name --abbrev-ref HEAD); sha=$(git rev-parse $branch); docker tag $npm_package_config_imageName $npm_package_config_imageRepo:$branch",
    "docker:tag:staging": "branch=$(git rev-parse --symbolic-full-name --abbrev-ref HEAD); sha=$(git rev-parse $branch); docker tag $npm_package_config_imageName $npm_package_config_imageRepo:$branch",
    "docker:tag:prod": " docker tag $npm_package_config_imageName $npm_package_config_imageRepo:$npm_package_version",
    "docker:push:prod": "docker push $npm_package_config_imageRepo:$npm_package_version",
    "docker:push:staging": "branch=$(git rev-parse --symbolic-full-name --abbrev-ref HEAD); sha=$(git rev-parse $branch); docker push $npm_package_config_imageRepo:$branch",
    "docker:push:prime": "branch=$(git rev-parse --symbolic-full-name --abbrev-ref HEAD); sha=$(git rev-parse $branch); docker push $npm_package_config_imageRepo:$branch",
    "dev": "nodemon ./index.js",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tylrhas/g5-asset-scraper.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tylrhas/g5-asset-scraper/issues"
  },
  "homepage": "https://github.com/tylrhas/g5-asset-scraper#readme",
  "dependencies": {
    "@canvas/image": "^1.0.1",
    "@supercharge/promise-pool": "^1.9.0",
    "axios": "^0.20.0",
    "blockhash-core": "^0.1.0",
    "cheerio": "^1.0.0-rc.5",
    "cloudinary": "^1.23.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "parse-address": "^1.1.2"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "nodemon": "^2.0.7"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "__tests__/config/"
    ]
  }
}
