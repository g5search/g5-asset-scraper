apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "asset-scraper.serviceAccountName" . }}
  labels:
    {{- include "asset-scraper.labels" . | nindent 4 }}
  annotations:
    iam.gke.io/gcp-service-account: "{{ include "asset-scraper.gsaName" . }}@{{ include "cloud-helper.values.projectID" $ }}.iam.gserviceaccount.com"
---
apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMServiceAccount
metadata:
  name: {{ include "asset-scraper.gsaName" . }}
  labels:
    {{- include "asset-scraper.labels" . | nindent 4 }}
---
apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPolicyMember
metadata:
  name: {{ include "asset-scraper.gsaName" . }}-workload-identity
  labels:
    {{- include "asset-scraper.labels" . | nindent 4 }}
spec:
  resourceRef:
    apiVersion: iam.cnrm.cloud.google.com/v1beta1
    kind: IAMServiceAccount
    name: {{ include "asset-scraper.gsaName" . }}
  role: "roles/iam.workloadIdentityUser"
  member: "serviceAccount:{{ include "cloud-helper.values.projectID" $ }}.svc.id.goog[{{ .Release.Namespace }}/{{ include "asset-scraper.serviceAccountName" . }}]"
